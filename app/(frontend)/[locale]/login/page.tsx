import { LoginForm } from "@molecules"
import { Container } from "@atoms"

export default async function LoginPage() {
    return (
        <Container>
            <LoginForm />
        </Container>
    )
}